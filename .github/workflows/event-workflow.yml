on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3
      - name: Install Composer Dependencies
        run: composer install -q --no-ansi --no-interraction --no-scripts --no-progress --prefer-dist

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: atopprojects.com
          username: atoppro@atopprojects.com
          password: G3V3ui5Oj)!bZ2
          server-dir: public_html/event.atopprojects.com/
